% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/visualization.R
\name{generate_gene_signature_boxplots}
\alias{generate_gene_signature_boxplots}
\title{Generate box plots for gene signatures (replaces barplot + SD)}
\usage{
generate_gene_signature_boxplots(
  seurat_obj,
  genes,
  out_dir,
  prefix,
  group_by_col,
  signature_name,
  condition_col = "condition",
  min_cells_per_group = 10L,
  add_jitter = TRUE
)
}
\arguments{
\item{seurat_obj}{Seurat object}

\item{genes}{Character vector of signature genes}

\item{out_dir}{Character. Output directory (e.g., group Heatmap folder)}

\item{prefix}{Character. File-prefix (e.g., "Main" or subset name)}

\item{group_by_col}{Character. Metadata column to group by (clusters, labels)}

\item{signature_name}{Character. Signature name for titles/filenames}

\item{condition_col}{Character. Metadata column holding the condition}

\item{min_cells_per_group}{Integer. Minimum cells in a group to plot}

\item{add_jitter}{Logical. If TRUE, overlay individual points (jittered).}
}
\value{
NULL, invisibly
}
\description{
For each group in `group_by_col`, produce a box plot showing the
distribution of expression (log-normalised counts) for each gene,
split by condition. This gives a much richer view of the data than
a bar plot + error bars, showing median, IQR, range, and outliers.
}
